<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">

        <shape>

            <corners android:radius="5dip" />

            <gradient
                android:angle="0"
                android:centerColor="@color/text_gray"
                android:centerY="0.75"
                android:endColor="@color/text_gray"
                android:startColor="@color/text_gray" />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">

        <clip>

            <shape>

                <corners android:radius="5dip" />

                <gradient
                    android:angle="0"
                    android:centerColor="@color/text_org"
                    android:centerY="0.75"
                    android:endColor="@color/text_org"
                    android:startColor="@color/text_org" />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">

        <clip>

            <shape>

                <corners android:radius="5dip" />

                <gradient
                    android:angle="0"
                    android:endColor="@color/text_org"
                    android:startColor="@color/text_org" />
            </shape>
        </clip>
    </item>

</layer-list>